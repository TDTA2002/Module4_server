import express from 'express'
const router = express.Router()

import userControll from '../../controllers/user.controll'

// router.get('/', userControll.getAllUser)
// router.get('/users', userControll.find)


export default router;
